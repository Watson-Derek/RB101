['ant', 'bear', 'caterpillar'].pop.size

# first, pop will remove the last item from the array and return that value
# then size will return the size, or length, of that value
# so the return value will be 11